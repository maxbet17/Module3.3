import { Reader, ReaderOptions } from '@subql/common';
import { Rule } from './rules';
export interface Report {
    name: string;
    skipped: boolean;
    description: string;
    valid: boolean;
}
export declare class Validator {
    private readonly reader;
    private readonly location;
    private readonly rules;
    static create(location: string, opts?: ReaderOptions): Promise<Validator>;
    constructor(reader: Reader, location: string);
    addRule(...rules: Rule[]): void;
    getValidateReports(): Promise<Report[]>;
    validate(): Promise<boolean>;
}
