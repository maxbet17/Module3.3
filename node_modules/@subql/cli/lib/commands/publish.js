"use strict";
// Copyright 2020-2022 OnFinality Limited authors & contributors
// SPDX-License-Identifier: Apache-2.0
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const fs_1 = require("fs");
const path_1 = (0, tslib_1.__importDefault)(require("path"));
const core_1 = require("@oclif/core");
const publish_controller_1 = require("../controller/publish-controller");
const build_1 = (0, tslib_1.__importDefault)(require("./build"));
const ACCESS_TOKEN_PATH = path_1.default.resolve(process.env.HOME, '.subql/SUBQL_ACCESS_TOKEN');
class Publish extends core_1.Command {
    async run() {
        var _a;
        const { flags } = await this.parse(Publish);
        const directory = flags.location ? path_1.default.resolve(flags.location) : process.cwd();
        if (!(0, fs_1.lstatSync)(directory).isDirectory()) {
            this.error('Argument `location` is not a valid directory');
        }
        // Ensure that the project is built
        try {
            await build_1.default.run(['--location', directory]);
        }
        catch (e) {
            this.log(e);
            this.error('Failed to build project');
        }
        let authToken;
        if (process.env.SUBQL_ACCESS_TOKEN) {
            authToken = process.env.SUBQL_ACCESS_TOKEN;
        }
        else if ((0, fs_1.existsSync)(ACCESS_TOKEN_PATH)) {
            try {
                authToken = (_a = process.env.SUBQL_ACCESS_TOKEN) !== null && _a !== void 0 ? _a : (0, fs_1.readFileSync)(ACCESS_TOKEN_PATH, 'utf8');
            }
            catch (e) {
                this.error(`Failed to read SUBQL_ACCESS_TOKEN from ${ACCESS_TOKEN_PATH}: ${e}`);
            }
        }
        else {
            this.error('Please provide SUBQL_ACCESS_TOKEN before publish');
        }
        this.log('Uploading SupQuery project to IPFS');
        const cid = await (0, publish_controller_1.uploadToIpfs)(directory, authToken.trim(), flags.ipfs).catch((e) => this.error(e));
        this.log(`SubQuery Project uploaded to IPFS: ${cid}`);
    }
}
exports.default = Publish;
Publish.description = 'Upload this SubQuery project to IPFS';
Publish.flags = {
    location: core_1.Flags.string({ char: 'l', description: 'local folder' }),
    ipfs: core_1.Flags.string({ description: 'IPFS gateway endpoint', required: false }),
};
//# sourceMappingURL=publish.js.map